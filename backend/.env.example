DATABASE_URL=postgresql://user:password@localhost:5432/bctigers
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
PORT=3000

# Frontend URL for password reset links
APP_URL=http://localhost:5173

# SMTP (optional — emails skipped if SMTP_HOST is unset)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@bctigers.ca
